#!/bin/sh
echo "-- Autogenerated by doc/8080_ops_gen.sh. Don't touch."
echo "return {"
cat doc/8080_ops.txt | awk -F'\t' 'BEGIN {OFS = FS} {
	if ($2 != "-") printf "\t[%s] = {%i, %i, %i},\n", $1, $3, $4, $5
}'
echo "}"
