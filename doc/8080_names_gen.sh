#!/bin/sh
echo "-- Autogenerated by doc/8080_names_gen.sh. Don't touch."
echo "return {"
cat doc/8080_ops.txt | awk -F'\t' 'BEGIN {OFS = FS} {
	if ($2 != "-") printf "\t[%s] = \"%s\",\n", $1, $2
}'
echo "}"
